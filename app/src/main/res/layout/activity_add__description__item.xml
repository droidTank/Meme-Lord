<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Add_Description_Item">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="5sp"
        app:layout_constraintEnd_toEndOf="parent"
        tools:ignore="MissingConstraints">



            <EditText
                android:id="@+id/Comic_descriptionrc"
                android:layout_width="wrap_content"
                android:layout_weight=".4"
                android:layout_height="wrap_content"
                android:layout_marginTop="2sp"
                android:hint="Comic description"
                android:inputType="text"

                android:maxLines="2" />

        <ImageView
            android:id="@+id/save_description"
            android:src="@drawable/ic_save_description"
            android:layout_marginRight="10sp"
            android:layout_width="30sp"
            android:layout_height="30sp" />

        <ImageView
            android:id="@+id/delete_description"
            android:layout_marginEnd="10sp"
            android:src="@drawable/ic_delete_description"
            android:layout_width="30sp"
            android:layout_height="30sp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>